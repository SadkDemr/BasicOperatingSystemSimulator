#ifndef CONTEXT_SWITCH_H_
#define CONTEXT_SWITCH_H_
#include<stdio.h>

class context_switch
{
private:
    /* data */
public:
    context_switch(/* args */);
    ~context_switch();
    context_switch Context;
    

    void context_switc	(	Context * 	current,Context * 	next )
};

context_switch::context_switch(/* args */)
{
}

context_switch::~context_switch()
{
}

#endif
